rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Not used by this project in Firestore-only image mode.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
